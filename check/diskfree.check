#!/bin/bash

check_diskfree()
##CHECKDSC check free disk space
#
##PARMNAME stdin
##PARMDESC mointpoints to skip, one regexp per line
##PARMDFLT none
{
    if [ -t 0 ]; then
	# stdin is terminal, so no stdin given
	# -> provide empty stdin, otherwise we will wait for terminal input...
	exec 0</dev/null
    fi
    perl check/wrapper/diskfree.pl
}
